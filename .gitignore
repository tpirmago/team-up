# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.*.local

# Dependencies and build output
node_modules
dist
dist-ssr
.vite
*.local

# TypeScript incremental build cache (created by `tsc -b` in frontend)
*.tsbuildinfo

# Test coverage reports (Vitest)
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets — never commit these
# Firebase Admin service account key, used by the backend
serviceAccountKey.json

# Generic certificates and private keys
*.pem
*.key
